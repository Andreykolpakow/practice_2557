Задание 1.
Сначала, читая методичку, написал через метод .group(). Потом дочитал до .groupdict() и решил переписать
код.

Задание 2.
Не трогал

Задание 3.
Сам декоратор написать получилось достаточно быстро.
С несколькими позиционными аргументами срабатывало, если аргументы immutable. Mutable объект не может быть
ключом словаря. Решил собрать результат в виде f-строки.
С именем функции пришлось немного повозиться.

Задание 4.
Спустя пару дней вернулся к решению домашки. Чуть мозг не сломал, восстанавливая в памяти, как работают
декораторы и решая 4-ое задание (