Задание 1.
После просмотра урока было просто

Задание 2.
То же, что в задании 1.

Задание 3.
В первом варианте (через zip) заметил непонятное: если учеников меньше, чем классов, всё отрабатывает как надо
без ошибок. Если же классов меньше, то работа программмы заканчивается ошибкой StopIteration. То есть, почему-то,
окончание итерации вылетает только если она окончится по второму списку. Интересно услышать Ваш комментаий по
этому вопросу.
С вариантом генератора пришлось повозиться)
А вот с "подумать, в каких ситуациях генератор даст эффект" - самое сложное. Пока не знаю, когда их стоит
применять.

Задание 4.
Идея была понятна, пришлось только повозиться с синтаксисом.
Конструкция result = [src[i] if src[i] > src[i-1] for i in range(len(src))] не хотела работать и вызывала
ошибку "expected 'else' after 'if' expression")))
После прочтения примеров из методички условие перенёс в конец, и всё заработало)

Задание 5.
Сначала решил через множества, но потом внимательно перечитал задание и увидел, что надо сохранить порядок чисел.
Сделал два варианта - через цикл и через List Comprehensions. Результаты неоднозначные.  На моём ноуте примерно
2 раза из десяти второй вариант проигрывает по времени, а 8 раз - выигрывает.
Потом перечитал методичку и снова вернулся к идее множеств. Добавил в список дважды числа от 100 до 10 000 и тогда
разница стала заметна невооруженным взглядом.