Задание 1.
В первом задании не понял, что значит "реализовать проверку порядка режимов". Он же (порядок) будет
именно таким, какой мы зададим?

Задание 2.
Пересматривая вебинар и читая методичку, сделал, немного заморачиваясь только с синтаксисом.

Задание 3.
Непонятным в задании было, как реализовать ссылание incomming на словарь. Решил, что в каждой организации есть что-то
подобное тарифной сетке, и захардкодил словарь.
Ещё одна сложность возникла уже с формрованием f-строк в get_total_income. Решение пришло не сразу - одинарный апостроф
заменил кавычками)

Задание 4.
Самое сложное было - это вписать в код классы SportCar и PolliceCar. Т.к. они дентичны родительскому. А без внутреннего
наполнения классы создавать, оказывается, нельзя. Остальное было делом техники.

Задание 5.
Ошибка была только в том, что при создании экземпляров классов не указал скобки с аргументом)))
Как это поправил - всё заработало)